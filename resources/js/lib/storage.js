// localStorage keys shared across the customer-facing pages (Card, MyCards,
// BottomNav) - kept in one place so the three stay in sync.
export const CUSTOMER_UUID_KEY = 'loyalty_uuid';
export const LAST_SHOP_SLUG_KEY = 'loyalty_last_shop_slug';
